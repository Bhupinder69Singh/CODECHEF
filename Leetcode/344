//reverse a string

class Solution {
public:
    void reverseString(vector<char>& name) {
    int s=0,e=name.size()-1;//to reverse we use 2 pointer for in place swapping
    while(s<e){
        swap(name[s],name[e]);
        s++;
        e--;
    }
    }
};
